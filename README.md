# Followers CMS — панель управления продуктами

Веб-админка, которая заменяет управление цифровыми продуктами через Google Sheets.
Каждый продукт = отдельный лист исходной таблицы; внутри — список **вариантов онбординга**
с тарифами, инапной сеткой (паками) и апсейлами.

Это **фронт-прототип**: данные хранятся в `localStorage` браузера и засеяны из таблицы
`Followers_Products.xlsx` (файл `seed-data.js`). Бэкенда пока нет — но структура данных уже
готова, чтобы в любой момент завести API/БД и синхронизировать вместо таблицы.

## Два режима — две роли

В сайдбаре вверху переключатель режимов. Это разделение зон ответственности:

| Режим | Кто работает | Что делает |
|-------|--------------|-----------|
| **💲 Продукты** | key-менеджер | Ведёт прайс-лист «как в Google-таблице»: цены, setup fee, trial, billing/variant, статус (вкл/выкл). Источник правды по продуктам. |
| **🧩 Онбординги** | продакт / QA | Собирает варианты онбордингов из продуктов (тарифы / паки / апсейлы). |

**Продукты** (новый раздел) повторяют структуру исходной таблицы: по листу-проекту
(UpFollow, Followerse, Wordbeat, Planly, GrowthPeak, Stage/Recurly, FollowBoost), внутри —
секции (Подписки / апсейлы / Инапная сетка / промо), у каждой свой цветовой акцент. В секции:

- **заголовки колонок** над списком: Продукт / Product ID, Цена, Период
  (месячный/недельный/дневной/разовый — выводится из цены), Сегмент, Статус;
- **клик по продукту** (или карандаш ✏️) открывает редактор со всеми полями (название,
  Product ID, цена, setup fee, trial, billing/variant, сегмент, статус, комментарий);
- **«+ строка»** добавляет продукт именно в эту секцию (открывается редактор, строка
  создаётся после «Сохранить»);
- **перетаскивание** за ручку `⠿` меняет порядок продуктов внутри секции;
- **пилюля статуса** переключает Вкл/Выкл по клику, без открытия редактора;
- **клик по заголовку секции** сворачивает/разворачивает её.

> Все продукты по умолчанию **выключены** — key-менеджер проходит и включает то, что реально live.

### Фильтры (кнопка «⛃ Фильтры»)

Готовые фасеты поверх текстового поиска и статуса (Все/Вкл/Выкл). Счётчик на кнопке
показывает число активных фильтров, «Сбросить фильтры» очищает их.

- **Продукты:** тип секции (Подписки / Апсейлы / Инапка-вебап / Промо), сегмент
  (орг / нон-орг / VAT / 1time), период (месячный / недельный / дневной / разовый),
  триал (с триалом / без), Billing ID (есть / нет).
- **Онбординги:** VAT (да / нет), локализация (вкл / выкл), наличие
  (с тарифами / с паками / с апсейлами).

**Связка:** в редакторе варианта онбординга поля *Product code* и *Billing ID* подсказывают
значения из продуктов того же проекта (автокомплит) — продакт собирает онбординг прямо из того,
что завёл key-менеджер.

## Запуск

```bash
cd followers-cms
python3 -m http.server 8000
# открыть http://localhost:8000
```

Либо просто открыть `index.html` в браузере.

## Доступ (демо)

```
логин:  admin
пароль: followers2026
```

Креды зашиты в `app.js` (`CREDS`) — это заглушка-гейт, а не настоящая авторизация.
При подключении бэкенда логин нужно перенести на сервер.

## Что умеет

- **4 продукта** в сайдбаре: UpFollow, Followerse, Wordbeat, Planly (с количеством вариантов).
- **Карточки вариантов** со статусом (Активен / На стопе), variant key, ссылкой и счётчиками
  тарифов/паков/апсейлов.
- **Поиск** по названию / variant key / ссылке и **фильтр** по статусу.
- **Редактор варианта** (боковая панель): статус, название, variant key, ссылка, флоу отмены,
  VAT, локализация + языки, логика начисления, и вложенные редактируемые списки:
  - **Тарифы подписки** — продукт / цена / billing ID (Recurly или Truegate);
  - **Инапная сетка (паки)** — название + product code;
  - **Апсейлы** — основной/скидочный варианты с product code.
- **Создание / удаление** вариантов.
- **Продукты** (режим key-менеджера): список по секциям, клик → редактор всех полей,
  добавление в конкретную секцию, перетаскивание порядка, быстрый тумблер Вкл/Выкл,
  поиск, фильтр по статусу, автосохранение.
- **Экспорт** онбордингов + продуктов в один JSON и **сброс** активного режима к исходным данным.

## Структура данных

Онбординги (`seed-data.js`):

```
products[]
  ├─ id, name, billingLabel
  └─ variants[]
       ├─ status, name, link, variantKey, vat, localizationOn, languages[],
       │  accrualLogic, cancelFlow
       ├─ plans[]   { name, price, billingId }
       ├─ packs[]   { name, code }
       └─ upsells[] { title, main, mainCode, discount, discountCode }
```

Продукты (`catalog-data.js`, сгенерирован из исходной таблицы; все `status` по умолчанию `off`):

```
products[]                       // лист таблицы = проект
  ├─ id, name
  └─ sections[]                  // Подписки / апсейлы / Инапная сетка / промо
       ├─ title
       └─ rows[] { name, productId, price, setupFee, trial,
                   billing, segment, status (on|off), comments }
```

## Файлы

| Файл           | Назначение                                            |
|----------------|-------------------------------------------------------|
| `index.html`   | разметка: экран логина, оболочка, редактор-дровер      |
| `styles.css`   | тёмная тема админки                                    |
| `app.js`       | вся логика: auth, режимы, рендер, редактирование, persist |
| `seed-data.js` | онбординги, сгенерированные из `Followers_Products.xlsx` |
| `catalog-data.js` | каталог-прайслист, сгенерирован из листов исходной таблицы |

## Следующие шаги (если решим продолжать)

1. Бэкенд + БД, REST/GraphQL вместо `localStorage`.
2. Реальная авторизация и роли.
3. История изменений / аудит (кто и что менял).
4. Прямая отдача конфигурации в приложения вместо ручного переноса из таблицы.
